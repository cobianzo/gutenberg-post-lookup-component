/**
 * Simple check to confirm that we are exporting the
 * <PostLookup> component.
 */

const PostLookup = require( './dist/post-lookup.js' );

console.log( PostLookup ); // Verifica si el componente es una función o un objeto válido
